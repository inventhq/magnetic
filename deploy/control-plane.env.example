# Magnetic Control Plane — Environment Variables
# Copy to /etc/magnetic/control-plane.env and fill in values

# Turso database (required for production)
TURSO_URL=libsql://your-db-name.turso.io
TURSO_TOKEN=your-turso-auth-token

# Civo auto-provisioning (optional — nodes can be registered manually)
CIVO_API_KEY=your-civo-api-key

# Cloudflare DNS (for Caddy wildcard TLS)
CF_API_TOKEN=your-cloudflare-api-token

# Overrides (defaults shown)
# PORT=4000
# MAGNETIC_DOMAIN=fujs.dev
# CADDY_ADMIN_URL=http://localhost:2019
# DB_PATH=magnetic-control-plane.db  (only used when TURSO_URL is not set)
