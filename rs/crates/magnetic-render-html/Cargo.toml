[package]
name = "magnetic-render-html"
version = "0.1.0"
edition = "2021"
description = "Render Magnetic DomNode trees to HTML strings"

[lib]
crate-type = ["lib"]

[dependencies]
magnetic-dom = { path = "../magnetic-dom" }
