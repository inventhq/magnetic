[package]
name = "magnetic-dom"
version = "0.1.0"
edition = "2021"
description = "Shared DomNode types for Magnetic renderers"

[lib]
crate-type = ["lib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
